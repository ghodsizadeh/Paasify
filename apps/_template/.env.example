# ==============================================================================
# Application Environment Configuration
# ==============================================================================
# Copy this file to .env and customize the values
# cp .env.example .env
# ==============================================================================

# Application Identity
APP_NAME=myapp
# APP_HOST is optional - if not set, defaults to ${APP_NAME}.${DEFAULT_DOMAIN}
# Uncomment below to use a custom domain instead of the auto-generated subdomain
# APP_HOST=myapp.example.com
APP_PORT=3000

# Docker Image
IMAGE=your-registry.com/your-app:latest

# Database Connections (using internal Docker network)
DATABASE_URL=postgres://admin:your-password@postgres:5432/myapp
REDIS_URL=redis://:your-password@redis:6379

# Application Secrets
# SECRET_KEY=your-secret-key-here
# API_KEY=your-api-key-here
