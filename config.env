# ==============================================================================
# PaaS Configuration
# ==============================================================================
# This is the central configuration file for your PaaS environment.
# All scripts and docker-compose files will source variables from here.
#
# IMPORTANT: Copy this file and customize for your environment:
#   cp config.env config.env.local
#   Edit config.env.local with your actual values
#   The setup script will use config.env.local if it exists
# ==============================================================================

# -----------------------------------------------------------------------------
# Domain & SSL Configuration (Single Source of Truth)
# -----------------------------------------------------------------------------
# DEFAULT_DOMAIN is the SSOT for auto-generated subdomains.
# Services without a custom domain will use: <service>.${DEFAULT_DOMAIN}
# Example: registry.example.com, traefik.example.com, portainer.example.com
DEFAULT_DOMAIN=example.com
ADMIN_EMAIL=admin@example.com

# Override specific service domains (optional - leave empty to use defaults)
# Uncomment and set if you need a custom domain for a specific service
# REGISTRY_DOMAIN=registry.${DEFAULT_DOMAIN}
# TRAEFIK_DOMAIN=traefik.${DEFAULT_DOMAIN}
# PORTAINER_DOMAIN=portainer.${DEFAULT_DOMAIN}

# -----------------------------------------------------------------------------
# Docker Registry
# -----------------------------------------------------------------------------
# Your self-hosted registry will be available at registry.${DEFAULT_DOMAIN}
REGISTRY_DOMAIN=registry.${DEFAULT_DOMAIN}
REGISTRY_USER=admin
# Generate a secure password: openssl rand -base64 32
REGISTRY_PASSWORD=changeme

# -----------------------------------------------------------------------------
# Traefik Dashboard
# -----------------------------------------------------------------------------
# Dashboard available at traefik.${DOMAIN}
TRAEFIK_USER=admin
# Generate with: htpasswd -nb admin yourpassword | sed 's/\$/\$\$/g'
TRAEFIK_PASSWORD_HASH='$$apr1$$ruca84Hq$$mbjdMZBAG.KWn7vfN/SNK/'

# -----------------------------------------------------------------------------
# Database Credentials
# -----------------------------------------------------------------------------
POSTGRES_USER=admin
POSTGRES_PASSWORD=changeme
POSTGRES_DB=main

REDIS_PASSWORD=changeme

# -----------------------------------------------------------------------------
# Paths
# -----------------------------------------------------------------------------
PAAS_ROOT=/opt/paas

# -----------------------------------------------------------------------------
# Backup Configuration
# -----------------------------------------------------------------------------
# Hetzner Object Storage (or S3-compatible)
# RESTIC_REPOSITORY=s3:https://fsn1.your-objectstorage.com/paas-backups
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# RESTIC_PASSWORD=your-encryption-password
